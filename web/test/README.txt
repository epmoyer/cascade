This folder contains automated tests using the py.test framework.
To execute, cd into this test directory (<project>/test) and run:

   py.test

If you don't have pytest installed, you can install it by running

   pip install pytest